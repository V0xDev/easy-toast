# 🔔 EasyToast: Уведомления на TS

**🔔 EasyToast** — это легковесная и настраиваемая библиотека для toast-уведомлений, написанная на чистом TypeScript (Vite) без внешних зависимостей.

## ![Логотип](/public/logo.avif)

---

## ✅ Особенности

- **Чистый TypeScript**: Написана на TS (стек Vite), не требует внешних библиотек.
- **Гибкая Настройка**: Настройка позиции, автозакрытия, лимита уведомлений и др.
- **Варианты Уведомлений**: Поддержка `success`, `error`, `warning`, `info` и `default` из коробки.
- **Простая Стилизация**: Легкое переопределение стилей через CSS переменные и слои (`@layer`).
- **Встроенные Иконки**: SVG-иконки для разных вариантов (`src/components/icons.ts`).
- **Настраиваемые параметры**: Пользовательские и глобальные настройки для каждого уведомления.

---

## 🚀 Быстрый старт

0.  **Клонируйте репозиторий в удобную для себя папку**
1.  **Интеграция файлов**:
    Скопируйте необходимые файлы в ваш проект:

    - `src/core/toast.ts`
    - `src/hooks/toast.ts`
    - `src/components/icons.ts`
    - `src/assets/toast.css`

2.  **Подключение стилей**:
    Импортируйте `toast.css` в ваш главный CSS-файл:

    ```css
    @import "путь/до/assets/toast.css";
    ```

---

## 💻 Как использовать

Библиотека предоставляет хук `useToast` (`src/hooks/toast.ts`) для удобного управления уведомлениями.

```ts
import { useToast } from "./hooks/toast";

// Получаем функции для создания тостов
const { standard, info, success, warning, error } = useToast();

// Пример вызова с кастомной конфигурацией
document.querySelector(".btnSuccess")?.addEventListener("click", () => {
  success("Заявка была успешно отправлена", {
    position: "top-right",
  });
});

// Пример другого типа
document.querySelector(".btnError")?.addEventListener("click", () => {
  error("Слишком много заявок, повторите позже...", {
    position: "bottom-right",
    closeOnClick: true,
  });
});

// Инфо
document.querySelector(".btnInfo")?.addEventListener("click", () => {
  info("Заявка была успешно добавлена", {
    position: "bottom-center",
  });
});
```

---

## 🎨 Кастомизация стилей (CSS)

Библиотека использует CSS-слои для удобного переопределения стилей без повышения специфичности.

Вы можете легко изменить переменные библиотеки, обернув свои стили в слой (например, main), который идет после слоя toast.

```css
/* 1. Объявляем слои */
@layer toast, main;

/* 2. Импортируем стили библиотеки в слой toast */
@import "toast.css" layer(toast);

/* 3. Переопределяем переменные в нашем слое main */
@layer main {
  :root {
    --toast-color: black; // Цвет текста уведомлений
  }
}
```

---

## ⚙️ Опции конфигурации (ToastConfig)

Вы можете передать объект конфигурации вторым аргументом в любую функцию useToast (success, info и т.д.).

- **closeDuration (number)**: Время в мс, через которое тост автоматически закроется. (По умолчанию: 3000)
- **autoClose (boolean)**: Включить автоматическое закрытие. (По умолчанию: true)
- **position (string)**: Позиция на экране. (По умолчанию: "top-right") Варианты: top-left, top-right, top-center, bottom-left, bottom-right, bottom-center.
- **limit (number)**: Максимальное количество одновременно отображаемых тостов. (По умолчанию: 5)
- **pauseOnHover (boolean)**: Ставить на паузу таймер автозакрытия при наведении мыши. (По умолчанию: true)
- **closeOnClick (boolean)**: Закрывать тост по клику на него. (По умолчанию: false)
- hideProgressbar (boolean): Скрыть полосу прогресса. (По умолчанию: false)

Пример переопределения:

```ts
warning("Этот тост будет висеть слева 10 секунд", {
  position: "top-left",
  closeDuration: 10000,
  pauseOnHover: false,
});
```

---

##### 📜 Лицензия

Проект распространяется под лицензией [MIT](https://opensource.org/license/MIT).

##### 💡 Вопросы или предложения?

Оставляйте их в Issues.
